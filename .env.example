# stdio mode does not need a network secret.
# HTTP mode: generate a 64-character lowercase hex value and store it in this file.
NETEASE_MCP_SECRET_FILE=/absolute/path/to/netease.secret
NETEASE_MCP_HOST=127.0.0.1
NETEASE_MCP_PORT=3303

# Optional account/playlist tools. Keep disabled until you explicitly opt in.
NETEASE_MCP_ACCOUNT_WRITE_ENABLED=0
NETEASE_MCP_COOKIE_FILE=/absolute/path/to/netease.session

# Optional label used by netease_status, for example desktop, server, android-termux.
NETEASE_MCP_DEVICE_KIND=desktop
